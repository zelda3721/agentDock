// Copyright (c) 2026 AgentDock Contributors
// SPDX-License-Identifier: Apache-2.0
{
  "module": {
    "name": "entry",
    "type": "entry",
    "description": "$string:entry_desc",
    "mainElement": "EntryAbility",
    // ADR-5：一套 App 包多端分发，三端共用同一 entry HAP，UI 由断点驱动（§8）
    "deviceTypes": [
      "phone",
      "tablet",
      "2in1"
    ],
    "deliveryWithInstall": true,
    "installationFree": false,
    "pages": "$profile:main_pages",
    "abilities": [
      {
        "name": "EntryAbility",
        "srcEntry": "./ets/entryability/EntryAbility.ets",
        "description": "$string:EntryAbility_desc",
        "icon": "$media:startIcon",
        "label": "$string:EntryAbility_label",
        "startWindowIcon": "$media:startIcon",
        "startWindowBackground": "$color:start_window_background",
        "exported": true,
        "skills": [
          {
            "entities": [
              "entity.system.home"
            ],
            "actions": [
              "action.system.home"
            ]
          }
        ]
      }
    ],
    // 记忆整理的 workScheduler 延迟任务承载（ADR-4/§6.3，T1.0-07）：系统在充电∧电量>40%∧空闲窗择机
    // 拉起本扩展的独立进程跑增量整理（EntryAbility.registerConsolidationWork 注册的 workId=1001）。
    // exported=false：只本应用的系统调度可拉起，外部不可调。
    "extensionAbilities": [
      {
        "name": "MemoryConsolidationExtension",
        "srcEntry": "./ets/workscheduler/MemoryConsolidationExtension.ets",
        "description": "$string:EntryAbility_desc",
        "type": "workScheduler",
        "exported": false
      }
    ],
    // 权限最小化（设计文档 §9.3）：V0.9 仅申请 INTERNET（远程 Provider / 模型下载）。
    // 文件读取一律走 filePicker 授权制，不申请全盘读权限。
    // 麦克风（ohos.permission.MICROPHONE）等语音相关权限属于 V1.5 语音子系统（§17/§18），届时再加，此处刻意不申请。
    "requestPermissions": [
      {
        // 判断当前是 Wi-Fi 还是蜂窝网，用于「仅 Wi-Fi 使用远程大模型」策略（§3.3 / T0.9-27 设置项）。
        // 只读网络类型，不读 SSID、不读位置——权限最小化（红线 18）。
        // networkPolicy='any'（默认）时根本不调该 API；wifi_only 下查询失败按 fail-closed 拒绝，
        // 宁可提示用户，也不替他在蜂窝网上花流量。
        "name": "ohos.permission.GET_NETWORK_INFO",
        "reason": "$string:permission_network_info_reason",
        "usedScene": {
          "abilities": [
            "EntryAbility"
          ],
          "when": "inuse"
        }
      },
      {
        "name": "ohos.permission.INTERNET",
        "reason": "$string:permission_internet_reason",
        "usedScene": {
          "abilities": [
            "EntryAbility"
          ],
          "when": "inuse"
        }
      }
    ]
  }
}
